import { PlaceholderScreen } from "../../../src/ui/PlaceholderScreen";

export default function IdeasIndexRoute() {
  return (
    <PlaceholderScreen
      eyebrow="02 · COLLAB"
      title="Ideas"
      body="A quiet place for the team's thinking. Coming next — the Collab tab from iOS, ported in its own sub-spec."
    />
  );
}
