import { PlaceholderScreen } from "../../../src/ui/PlaceholderScreen";

export default function SearchIndexRoute() {
  return (
    <PlaceholderScreen
      eyebrow="04 · LOOK FOR"
      title="Search"
      body="Cross-tab search lands here — sessions, ideas, members, files. Implemented after the other tabs catch up."
    />
  );
}
