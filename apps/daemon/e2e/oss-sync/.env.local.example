# cloud FC 测试账号（owner，用于建 team/invite）
TEST_EMAIL=oss-e2e@example.com
TEST_PASSWORD=replace-me
# cloud FC 基址（默认线上）
CLOUD_API_URL=https://cloud.ucar.cc
# 设为 1 才跑可选三节点用例
RUN_THREE_NODE=0
# host 端口不再需要：harness 每个用例用独立 compose project + 临时端口（容器内固定
# 8787），跑完用 `docker compose port` 动态发现，故场景可安全并行。
