# cloud FC 测试账号（owner，用于建 team/invite）
TEST_EMAIL=oss-e2e@example.com
TEST_PASSWORD=replace-me
# cloud FC 基址（默认线上）
CLOUD_API_URL=https://cloud.ucar.cc
# 两个 daemon 容器的 host 映射端口（容器内固定 8787）
HOST_PORT_A=18081
HOST_PORT_B=18082
HOST_PORT_C=18083
# 设为 1 才跑可选三节点用例
RUN_THREE_NODE=0
